export const metadata = {
  title: {
    default: 'Zertifizierung für handgemachte Produkte | Made by Human',
    template: '%s',
  },
};

export default function Layout({ children }) {
  return children;
}
