export const metadata = {
  title: {
    default: 'Handarbeit mit Maschinen – geht das? | Made by Human',
    template: '%s',
  },
};

export default function Layout({ children }) {
  return children;
}
