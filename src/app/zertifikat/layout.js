export const metadata = {
  title: 'Öffentlicher Zertifizierungsdatensatz',
  robots: {
    index: false,
    follow: true,
  },
};

export default function CertificateLookupLayout({ children }) {
  return children;
}
